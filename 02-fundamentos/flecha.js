// function sumar (a, b) {
//     return a + b
// }

// console.log( sumar(10 , 5) );


// normal
// const sumar = (a, b) => {
//     return a + b;
// }

// Pero como solo esta returnado un valor podemos simplificarlo
const sumar = (a, b) => a + b; 
console.log( sumar(10 , 5) );

const saludar = () => 'hola mundo';
console.log(saludar());